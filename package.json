{
  "name": "paadal-pettagam",
  "private": true,
  "version": "1.0.0",
  "description": "Carnatic song notebook \u2014 tracks every song learned, with transliteration, duplicate alerts, lyrics PDFs, audio, and voice input.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}